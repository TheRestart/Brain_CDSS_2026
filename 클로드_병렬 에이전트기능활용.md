# 클로드 병렬 에이전트 기능 활용 가이드

## 개요

Claude Code는 여러 에이전트를 **동시에 병렬로 실행**하여 복잡한 작업을 효율적으로 처리할 수 있는 기능을 제공합니다.

---

## 왜 병렬 에이전트 기능이 필요한가?

### 1. 대규모 코드베이스의 복잡성
현대 소프트웨어 프로젝트는 수백, 수천 개의 파일로 구성됩니다. 하나의 에이전트가 순차적으로 모든 파일을 분석하면 **시간이 오래 걸리고 비효율적**입니다.

### 2. 작업의 독립성 활용
소프트웨어 개발에서 많은 작업은 서로 독립적입니다:
- 프론트엔드 분석 ↔ 백엔드 분석
- 테스트 코드 검색 ↔ 소스 코드 검색
- 문서 탐색 ↔ 설정 파일 분석

이러한 **독립적 작업을 동시에 처리**하면 전체 작업 시간을 크게 단축할 수 있습니다.

### 3. 사용자 대기 시간 최소화
개발자의 시간은 소중합니다. 병렬 처리를 통해:
- **응답 속도 향상**: 여러 검색을 동시에 수행
- **생산성 증가**: 대기 시간 감소로 더 많은 작업 처리
- **작업 흐름 유지**: 빠른 피드백으로 개발 흐름 방해 최소화

### 4. 복잡한 질문에 대한 종합적 답변
"이 프로젝트의 전체 구조를 분석해줘"와 같은 요청은 여러 영역을 동시에 살펴봐야 합니다:
```
┌─────────────────────────────────────────────┐
│  단일 요청 → 다중 병렬 분석                  │
│  ├─ 컴포넌트 구조 분석                       │
│  ├─ 라우팅 구조 분석                         │
│  ├─ 상태 관리 분석                           │
│  └─ API 통신 구조 분석                       │
│                                             │
│  → 종합된 전체 구조 리포트 제공              │
└─────────────────────────────────────────────┘
```

### 5. 리소스의 효율적 활용
병렬 처리는 컴퓨팅 리소스를 **최대한 활용**합니다. 순차 처리 시 발생하는 유휴 시간을 줄이고, 동시에 여러 작업을 처리하여 전체 효율성을 높입니다.

---

## 사용 가능한 에이전트 유형

| 에이전트 | 설명 | 주요 용도 |
|----------|------|-----------|
| **Explore** | 코드베이스 탐색 전문 | 파일 패턴 검색, 키워드 검색, 코드 구조 분석 |
| **Plan** | 소프트웨어 아키텍트 | 구현 전략 설계, 단계별 계획 수립 |
| **Bash** | 명령어 실행 전문 | Git 작업, 빌드, 터미널 명령 |
| **general-purpose** | 범용 에이전트 | 복잡한 멀티스텝 작업, 리서치 |

---

## 병렬 실행 원리

### 병렬 실행 가능 조건
- 작업들이 **서로 독립적**일 때
- 작업 간 **의존성이 없을** 때
- **동시 처리**가 효율적일 때

### 순차 실행이 필요한 경우
- 작업 A의 결과가 작업 B에 필요한 경우
- 예: 빌드 → 테스트 (빌드 완료 후 테스트 실행)

---

## 활용 예시

### 예시 1: 다중 폴더 분석

**요청:**
```
"src/pages 폴더와 src/components 폴더 구조를 동시에 분석해줘"
```

**내부 처리:**
```
┌─────────────────────────────────────────┐
│  병렬 에이전트 실행                      │
│  ├─ Explore Agent 1: src/pages 분석     │
│  └─ Explore Agent 2: src/components 분석│
│                                         │
│  → 두 결과를 합쳐서 최종 응답 제공       │
└─────────────────────────────────────────┘
```

### 예시 2: 코드 검색 + 문서 검색

**요청:**
```
"API 엔드포인트 사용처와 관련 문서를 동시에 찾아줘"
```

**내부 처리:**
```
┌─────────────────────────────────────────┐
│  병렬 에이전트 실행                      │
│  ├─ Agent 1: 코드에서 API 사용처 검색    │
│  └─ Agent 2: 문서/README 검색           │
└─────────────────────────────────────────┘
```

### 예시 3: 프론트엔드 + 백엔드 동시 분석

**요청:**
```
"프론트엔드 컴포넌트와 백엔드 API를 병렬로 분석해줘"
```

**내부 처리:**
```
┌─────────────────────────────────────────┐
│  병렬 에이전트 실행                      │
│  ├─ Agent 1: Frontend 구조 분석         │
│  └─ Agent 2: Backend API 분석           │
└─────────────────────────────────────────┘
```

---

## 병렬 실행 요청 방법

### 명시적 요청
다음과 같은 키워드를 사용하면 병렬 처리를 명시적으로 요청할 수 있습니다:

- "병렬로 실행해줘"
- "동시에 분석해줘"
- "parallel로 처리해줘"
- "함께 검색해줘"

### 자동 병렬 처리
Claude는 다음 상황에서 자동으로 병렬 처리를 판단합니다:

1. 여러 독립적인 검색 요청
2. 다중 폴더/파일 분석 요청
3. 서로 관련 없는 작업들의 동시 요청

---

## 백그라운드 실행

에이전트를 **백그라운드에서 실행**할 수도 있습니다:

- 즉시 결과가 필요하지 않은 장시간 작업
- 다른 작업을 하면서 동시에 처리하고 싶은 경우
- 결과는 나중에 확인 가능

---

## 성능 이점

| 처리 방식 | 소요 시간 (예시) |
|-----------|------------------|
| 순차 실행 | 작업1(3초) + 작업2(3초) = 6초 |
| 병렬 실행 | max(작업1, 작업2) = 3초 |

→ **병렬 실행으로 약 50% 시간 단축** (독립 작업 기준)

---

## 주의사항

1. **의존성 있는 작업**은 자동으로 순차 처리됩니다
2. **리소스 제한**으로 인해 동시 실행 에이전트 수에 제한이 있을 수 있습니다
3. 결과는 모든 에이전트 완료 후 **통합**되어 제공됩니다

---

## 실전 활용 팁

### DO (권장)
- ✅ 독립적인 파일/폴더 분석 시 병렬 요청
- ✅ 여러 검색 조건을 동시에 처리할 때 활용
- ✅ 대규모 코드베이스 탐색 시 영역별 병렬 분석

### DON'T (비권장)
- ❌ 결과 의존성이 있는 작업에 병렬 강제
- ❌ 단일 작업을 불필요하게 분할하여 병렬 처리
- ❌ 순서가 중요한 작업에 병렬 적용

---

## 요약

| 항목 | 내용 |
|------|------|
| 기능 | 여러 에이전트를 동시에 병렬 실행 |
| 이점 | 작업 시간 단축, 효율성 향상 |
| 조건 | 독립적이고 의존성 없는 작업 |
| 요청 방법 | "병렬로", "동시에" 키워드 사용 또는 자동 판단 |

---

*문서 작성일: 2026-02-03*
